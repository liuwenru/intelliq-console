<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:rabbit="http://www.springframework.org/schema/rabbit"
       xsi:schemaLocation="http://www.springframework.org/schema/rabbit
           https://www.springframework.org/schema/rabbit/spring-rabbit.xsd
           http://www.springframework.org/schema/beans
           https://www.springframework.org/schema/beans/spring-beans.xsd">

<!--    <rabbit:connection-factory id="connectionFactory" virtual-host="/" connection-timeout="5000" host="192.168.203.221" username="epoint" password="epoint"/>-->
<!--    <rabbit:template id="amqpTemplate" connection-factory="connectionFactory"/>-->
<!--    <rabbit:admin connection-factory="connectionFactory"/>-->
<!--    <rabbit:queue name="myqueue"/>-->
        <rabbit:connection-factory id="rabbitConnectionFactory"
                                   connection-factory="clientConnectionFactory"
                                   host="192.168.188.109"
                                   port="5671"
                                   virtual-host="/"
                                   username="epoint" password="epoint" />

        <bean id="clientConnectionFactory"
              class="org.springframework.amqp.rabbit.connection.RabbitConnectionFactoryBean">
            <property name="useSSL" value="true" />
            <property name="sslPropertiesLocation" value="file:/home/ijarvis/workspace/javaWorkSpace/intelliq-console/src/main/resources/rabbitSSL.properties"/>
        </bean>
        <rabbit:template id="amqpTemplate" connection-factory="rabbitConnectionFactory"/>
        <rabbit:admin connection-factory="rabbitConnectionFactory"/>
        <rabbit:queue name="myqueue"/>

</beans>